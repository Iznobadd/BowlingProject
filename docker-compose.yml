version: '3'
services:
  angular-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - GITHUB_REPO=<URL_DU_DEPOT>
    ports:
      - 8080:80
    restart: always
    network_mode: host
